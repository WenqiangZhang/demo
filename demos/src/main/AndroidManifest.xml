<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.goodev.retrofitdemo"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.WelcomActivity"
            android:label="@string/app_name"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="org.goodev.retrofitdemo.activity.RetrofitActivity"
            android:label="官方的Retrofit范例"
            android:launchMode="singleTop"></activity>

        <activity
            android:name="org.goodev.retrofitdemo.activity.MyRetrofitDemoActivity"
            android:label="我的Retrofit范例"
            android:launchMode="singleTop"></activity>
    </application>

</manifest>